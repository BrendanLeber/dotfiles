Installation:

    git clone git://github.com/BrendanLeber/dotvim.git dotvim

Create symlinks:

    mklink _vimrc dotvim/vimrc
    mklink _gvimrc dotvim/gvimrc

Switch to the `dotvim` directory, and fetch submodules:

    cd dotvim
    git submodule update --init
