Installation:

    cd %UserProfile%
    git clone git://github.com/BrendanLeber/dotvim.git vimfiles

Create symlinks:

    mklink _vimrc vimfiles/vimrc
    mklink _gvimrc vimfiles/gvimrc

Switch to the `vimfiles` directory, and fetch submodules:

    cd vimfiles
    git submodule update --init
